# E8-T3 — Implementar Upload de Documentos

## Objetivo
Implementar funcionalidade crítica de upload de documentos no frontend e backend. Usuários precisam conseguir adicionar documentos ao sistema.

## Dependências
- `E8-T1` (Sincronizar endpoints - para confirmar backend está preparado)

## Entregáveis
**Legenda de status:** `[x]` Concluído · `[~]` Parcial · `[ ]` Pendente

### Backend
- [ ] Endpoint `POST /api/documents/upload` implementado
- [ ] Validação de arquivo (tipo, tamanho)
- [ ] Verificação de limite por plano
- [ ] Rate limiting (5 uploads/min)
- [ ] Response 202 Accepted com `documentId` e status
- [ ] Async processing via Celery + documentação

### Frontend
- [ ] `DocumentUploadComponent` criado com drag-and-drop
- [ ] Validação de tipo/tamanho no cliente
- [ ] Progress bar visual durante upload
- [ ] Botão na página de documentos para iniciar upload
- [ ] Lista de documentos atualizada após upload
- [ ] Feedback de sucesso/erro para usuário
- [ ] Testes unitários

## Especificação do Endpoint Backend

### POST /api/documents/upload

**Request (multipart/form-data):**
```
Content-Type: multipart/form-data

Fields:
  - file: File (obrigatório, ≤ 100MB)
    Tipos aceitos: PDF, DOCX, TXT, MD
  - workspaceId: string (opcional)
  - title: string (opcional, usa nome do arquivo se não informado)
```

**Response (202 Accepted):**
```json
{
  "data": {
    "documentId": "uuid",
    "title": "Meu Documento",
    "status": "pending",
    "message": "Upload iniciado. Processamento em andamento."
  }
}
```

**Validações Backend:**
- Arquivo ≤ 100MB (retornar 413 se exceder)
- Tipo MIME deve estar em lista branca (PDF, DOCX, TXT, MD)
- Verificar quota do usuário (limite de documentos por plano)
- Rate limiting: máximo 5 uploads/min por usuário
- Salvar arquivo em S3/MinIO
- Iniciar job Celery para processamento

**Error Responses:**
```json
{
  "error": "File too large",
  "max_size_mb": 100,
  "received_size_mb": 150
}
```

```json
{
  "error": "Invalid file type",
  "accepted_types": ["application/pdf", "application/vnd.openxmlformats-officedocument.wordprocessingml.document"]
}
```

```json
{
  "error": "Document quota exceeded",
  "limit": 50,
  "current": 50,
  "plan": "free"
}
```

## Implementação Frontend

### 1. DocumentUploadComponent (Standalone)

**O que Implementar:**
- [ ] Componente standalone Angular
- [ ] Zona drag-and-drop com visual feedback
- [ ] Validação de tipo/tamanho no cliente **antes** de fazer upload
- [ ] Seletor de arquivo via input (fallback)
- [ ] Progress bar animado durante upload
- [ ] Mensagem de status (uploading, processing, success, error)
- [ ] Botão de cancelar (se aplicável)

**Estrutura:**
```
src/app/features/documents/
├── components/
│   └── document-upload/                    [NOVO]
│       ├── document-upload.component.ts
│       ├── document-upload.component.html
│       ├── document-upload.component.scss
│       └── document-upload.component.spec.ts
```

**Exemplo de Código:**
```typescript
@Component({
  selector: 'app-document-upload',
  standalone: true,
  imports: [CommonModule, MaterialModule],
  templateUrl: './document-upload.component.html',
  styleUrls: ['./document-upload.component.scss']
})
export class DocumentUploadComponent {
  @Output() uploadComplete = new EventEmitter<UploadResponse>();

  uploadProgress = 0;
  isUploading = false;
  errorMessage = '';

  constructor(private documentsService: DocumentsService) {}

  onDragOver(event: DragEvent) {
    event.preventDefault();
    // Add visual feedback
  }

  onFileDrop(event: DragEvent) {
    event.preventDefault();
    const files = event.dataTransfer?.files;
    if (files) {
      this.handleFiles(files);
    }
  }

  onFileSelected(event: Event) {
    const input = event.target as HTMLInputElement;
    if (input.files) {
      this.handleFiles(input.files);
    }
  }

  private handleFiles(files: FileList) {
    const file = files[0];

    // Client-side validation
    const maxSize = 100 * 1024 * 1024; // 100MB
    const allowedTypes = ['application/pdf', 'application/vnd.openxmlformats-officedocument.wordprocessingml.document', 'text/plain', 'text/markdown'];

    if (file.size > maxSize) {
      this.errorMessage = `Arquivo muito grande (máx: 100MB)`;
      return;
    }

    if (!allowedTypes.includes(file.type)) {
      this.errorMessage = `Tipo não permitido. Aceitos: PDF, DOCX, TXT, MD`;
      return;
    }

    this.uploadFile(file);
  }

  private uploadFile(file: File) {
    this.isUploading = true;
    this.errorMessage = '';

    this.documentsService.uploadDocument(file)
      .subscribe({
        next: (response) => {
          this.uploadComplete.emit(response);
          this.isUploading = false;
        },
        error: (error) => {
          this.errorMessage = error.error.error || 'Erro ao fazer upload';
          this.isUploading = false;
        }
      });
  }
}
```

### 2. UploadProgressComponent

**O que Implementar:**
- [ ] Componente para exibir progress bar
- [ ] Animação suave da barra
- [ ] Percentual de progresso
- [ ] Mensagem de status
- [ ] Tempo estimado (se disponível)

### 3. Integração em DocumentsPageComponent

**O que Fazer:**
- [ ] Adicionar `<app-document-upload>` à página
- [ ] Modal ou seção expansível para upload
- [ ] Ao upload completar, recarregar lista de documentos
- [ ] Exibir documento recém-adicionado no topo da lista

**Template HTML:**
```html
<div class="documents-container">
  <div class="header">
    <h2>Meus Documentos</h2>
    <button (click)="openUploadModal()">+ Upload Documento</button>
  </div>

  <!-- Upload Modal -->
  <mat-dialog #uploadDialog>
    <h2 mat-dialog-title>Adicionar Documento</h2>
    <mat-dialog-content>
      <app-document-upload (uploadComplete)="onUploadComplete($event)"></app-document-upload>
    </mat-dialog-content>
  </mat-dialog>

  <!-- Documentos List -->
  <mat-table [dataSource]="documents">
    <!-- ... colunas da tabela -->
  </mat-table>
</div>
```

## Orientações de Implementação

### Backend
1. **Criar Handler de Upload**
   - Receber `multipart/form-data`
   - Validar arquivo antes de salvar
   - Salvar em S3/MinIO com path organizado por user_id

2. **Integração Celery**
   - Criar task `process_document.py`
   - Task deve: extrair texto, chunkar, vetorizar
   - Atualizar status de documento em tempo real

3. **Database**
   - Adicionar campos se necessário: `file_path`, `file_size`, `original_filename`
   - Considerar soft delete (não físico)

### Frontend
1. **HTTP Interceptor para Upload**
   - Capturar eventos de progress (`HttpProgressEvent`)
   - Atualizar barra de progresso em tempo real

2. **Tratamento de Erro Robusto**
   - Validação dual (cliente + servidor)
   - Mensagens claras para usuário
   - Retry automático para erros de rede?

3. **UX/Accessibility**
   - Drag-and-drop + input file (fallback)
   - Disabled durante processamento
   - ARIA labels para acessibilidade

## Padrões de Código Obrigatórios
- Usar FormData API para multipart/form-data
- Implementar `HttpClient.post()` com `reportProgress: true`
- Validação dual (cliente + servidor)
- Tratamento de AbortController para cancelamento
- Documentar limites (100MB) e tipos aceitos
- Usar OnDestroy para unsubscribe de observables
- Logging estruturado de erros

## Testes Obrigatórios
**Legenda de status:** `[x]` Concluído · `[~]` Parcial · `[ ]` Pendente

### Frontend
- [ ] Teste: Componente rejeita arquivo > 100MB antes de enviar
- [ ] Teste: Componente rejeita tipo não aceito (ex: .exe)
- [ ] Teste: Progress bar atualiza corretamente
- [ ] Teste: Erro do servidor é exibido ao usuário
- [ ] Teste: uploadComplete emit ao sucesso
- [ ] Teste: Lista é atualizada após upload

### Backend
- [ ] Teste: 413 Payload Too Large se arquivo > 100MB
- [ ] Teste: 400 Bad Request se tipo não permitido
- [ ] Teste: 429 Too Many Requests se > 5 uploads/min
- [ ] Teste: 402 Payment Required se quota excedida
- [ ] Teste: 202 Accepted com documentId válido
- [ ] Teste: Arquivo salvo corretamente em S3/MinIO
- [ ] Teste: Job Celery enfileirado corretamente

## Estimativa
**Responsável:** Backend Team (3-4 dias) + Frontend Team (2-3 dias)
**Deadline:** 19 de Novembro (1 semana)
**Status:** ⏳ Não Iniciado
**Prioridade:** 🔴 CRÍTICA - Bloqueia fluxo principal

## Links Relacionados
- [Endpoints_Pendentes.md](../frontend/status/Endpoints_Pendentes.md#21-upload-de-documento)
- [agents_vector_dev.md](../frontend/agents_vector_dev.md)
- CLAUDE.md — Padrões de código do projeto
