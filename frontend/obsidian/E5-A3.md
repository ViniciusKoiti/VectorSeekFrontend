---
id: E5-A3
epic: "Épico 5 — Chat Conversacional & Experiências Multimodais"
status: backlog
tags:
  - frontend
  - angular
  - chat
  - ux
  - obsidian/backlog
dependencies:
  - E5-A2
obsidian-link: "[[E5-A3 Construir lista de conversas]]"
---

# E5-A3 · Construir lista de conversas

## Descrição técnica
- Criar `ConversationListComponent` em `apps/platform/src/app/chat/components` com `ChangeDetectionStrategy.OnPush`.
- Consumir `ChatStore` para renderizar conversas ordenadas por `lastMessageAt` usando `async pipe` + signals.
- Implementar busca incremental (filtro por texto) e estado vazio/carregando.
- Utilizar `cdkVirtualScrollViewport` para suportar grandes volumes com item size configurável.
- Adicionar ação de criação via `MatDialog` abrindo `CreateConversationDialogComponent` com formulário reativo.
- Registrar rotas e providers necessários em `ChatModule` com lazy loading (`loadComponent`).

## Dependências
- `E5-A2` disponível (store e dados normalizados).

## Critérios de aceite
- Lista renderiza dados reais e atualiza badge de mensagens não lidas em tempo real.
- Criação de conversa dispara `ChatStore.createConversation` e fecha modal após sucesso.
- Layout responsivo com suporte a dark mode (Tailwind + Angular Material).

## Checklist sugerido
- [ ] Implementar componente de lista com template e estilos.
- [ ] Integrar virtual scroll e estados de carregamento.
- [ ] Criar modal de nova conversa com validações.
- [ ] Conectar ações ao `ChatStore`.
- [ ] Adicionar testes de componente com `ComponentFixture` e mocks do store.
